# Dockerfile para testing - Simula el ambiente de CI
FROM node:20-alpine

WORKDIR /app

# Copiar archivos de dependencias
COPY package*.json ./

# Instalar dependencias (incluyendo devDependencies para tests)
RUN npm ci

# Copiar código fuente
COPY . .

# Comando por defecto: ejecutar tests
CMD ["npm", "test"]
